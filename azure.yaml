name: todo-nodejs-mongo-aca
services:
  - name: ${AZURE_ENV_NAME}api
    project: src/api
    language: js
    host: containerapp
  - name: ${AZURE_ENV_NAME}web
    project: src/web
    dist: build
    language: js
    host: appservice